{
  "name": "pizza_restaurant_reporter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/reporterService.js"
  },
  "keywords": [],
  "author": "sebmaz93",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.10.3",
    "mongodb": "^5.4.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.1",
    "@types/node": "^18.16.3",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  }
}
