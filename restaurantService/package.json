{
  "name": "pizza_restaurant",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/restaurantService.js"
  },
  "keywords": [],
  "author": "sebmaz93",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.10.3"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.1",
    "@types/node": "^18.16.3",
    "typescript": "^5.0.4",
    "rimraf":"^5.0.0"
  }
}
